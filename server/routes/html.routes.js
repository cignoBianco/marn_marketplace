import express from 'express'
//import htmlCtrl from '../controllers/html.controller'

const router = express.Router()

//router.route('/auth/signout')
  //.get(authCtrl.signout)

export default router
